/*#include <wx/wx.h>

class MyFrame : public wxFrame
{
public:
    MyFrame(const wxString& title)
        : wxFrame(NULL, wxID_ANY, title, wxDefaultPosition, wxSize(400, 300))
    {
        // Adicione os elementos da interface gráfica aqui

        Centre();
    }
};

class MyApp : public wxApp
{
public:
    virtual bool OnInit()
    {
        MyFrame* frame = new MyFrame("Minha Aplicação");
        frame->Show(true);
        return true;
    }
};

wxIMPLEMENT_APP(MyApp);
*/